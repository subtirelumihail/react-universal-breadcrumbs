import React, { useState, useEffect } from 'react'
import useReactPath from './hooks/useReactPath'

const Breadcrumb: React.FC = ({ path: string = '' }) => {
  return (
    <div className="bread">
      <h2>Do cool stuff</h2>
    </div>
  )
}

export default Breadcrumb
